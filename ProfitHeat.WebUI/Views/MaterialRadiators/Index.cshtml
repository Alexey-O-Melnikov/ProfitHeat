@model IEnumerable<ProfitHeat.Domain.MaterialRadiator>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TitleMaterialRadiator)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TitleMaterialRadiator)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.MaterialRadiatorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.MaterialRadiatorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.MaterialRadiatorID })
        </td>
    </tr>
}

</table>
