@model IEnumerable<ProfitHeat.Domain.Glass>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HeatResistanceCoefficient)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Type)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HeatResistanceCoefficient)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.GlassID }) |
            @Html.ActionLink("Details", "Details", new { id=item.GlassID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.GlassID })
        </td>
    </tr>
}

</table>
