@model IEnumerable<ProfitHeat.Domain.Radiator>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.RadiatorType)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Material)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PowerSection)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.RadiatorType)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Material)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PowerSection)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.RadiatorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.RadiatorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.RadiatorID })
        </td>
    </tr>
}

</table>
