@model List<ProfitHeat.Domain.Glass>

<div class="@*form-group*@">
    <label for="glasses" class="control-label">Стеклопакет:</label>
    <div class="">
        <input type="text" list="glasses"  class="form-control" value="@ViewBag.Glass" />
        <datalist id="glasses">
            @foreach (var item in Model)
            {
                <option>@item.Type</option>
            }
        </datalist>
    </div>
</div>