@model IEnumerable<ProfitHeat.Domain.ManufacturerRadiator>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TitleCompany)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TitleCompany)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ManufacturerRadiatorID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ManufacturerRadiatorID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ManufacturerRadiatorID })
        </td>
    </tr>
}

</table>
